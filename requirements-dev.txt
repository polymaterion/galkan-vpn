# Test-only dependencies. Installed locally / in CI, NOT baked into the
# billing production image (see infra/Dockerfile.billing, which installs
# only requirements.billing.txt).
-r requirements.billing.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
respx==0.21.1
